
<div class="level">
  <h1 class="title">Editing Sprint</h1>
  <div class="level-item-right">
    <%= link_to @sprint, class: "button" do %>
      <span class="icon is-small">
        <%= icon('fas', 'eye') %>
      </span>
      <span>Show</span>
    <% end %>
    <%= render partial: "partials/link_btn", locals: {href: sprints_path, icon: 'caret-left', text: 'Back', css_class: 'back-btn'} %>
  </div>
</div>

<%= render partial: 'form', locals: {tag: @sprint} %>
