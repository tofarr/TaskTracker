<%if tag %>
  <div class="tag is-medium" style="background-color: <%=tag.color%>; color: <%=text_color tag.color %>">
    <%= render partial: "partials/icon", locals: {img: tag.icon, size: 24} %>
    <span>
      <%=tag.title%>
    </span>
  </div>
<% end %>
